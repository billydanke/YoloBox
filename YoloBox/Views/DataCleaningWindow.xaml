<Window x:Class="YoloBox.Views.DataCleaningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YoloBox.Models"
        xmlns:converters="clr-namespace:YoloBox.Converters"
        xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
        mc:Ignorable="d"
        Title="DataCleaningWindow"
        Background="#202020"
        Height="450" MinHeight="450" Width="800" MinWidth="800">

    <Window.Resources>
        <converters:BooleanToRedGreenColorConverter x:Key="BooleanToRedGreenColorConverter" />
    </Window.Resources>
    
    <Grid>

        <ListView ItemsSource="{Binding ImageList}"
                  Background="Transparent"
                  ScrollViewer.CanContentScroll="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Focusable" Value="False" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                    <Setter Property="OverridesDefaultStyle" Value="True" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <vwp:VirtualizingWrapPanel/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="{Binding IsIncluded, Converter={StaticResource BooleanToRedGreenColorConverter}}" Background="#404040" BorderThickness="3" Margin="3" CornerRadius="10" Padding="5">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="RightClick" Command="{Binding ToggleIncludedCommand}" />
                        </Border.InputBindings>
                        <Image Source="{Binding Image}" Width="400" Stretch="Uniform"/>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Content="Load Image Folder" Command="{Binding OpenImageFolderCommand}" HorizontalAlignment="Left" VerticalAlignment="Top" />
    </Grid>
</Window>
