<UserControl x:Class="YoloBox.Components.ResizableLabelBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YoloBox.Components"
             xmlns:converters="clr-namespace:YoloBox.Converters"
             mc:Ignorable="d" 
             Width="Auto" Height="Auto"
             IsHitTestVisible="{Binding IsSelectable}"
             x:Name="Root">

    <UserControl.Resources>
        <converters:ObjectEqualityConverter x:Key="ObjectEqualityConverter" />
        <converters:ObjectEqualityToVisibilityConverter x:Key="ObjectEqualityToVisibilityConverter" />

        <Style x:Key="ResizableThumbStyle" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Width" Value="10"/>
            <Setter Property="Height" Value="10"/>
            <Setter Property="Background" Value="{Binding ClassBrush}"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="5" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <UserControl.InputBindings>
        <MouseBinding Gesture="LeftClick" Command="{Binding DataContext.SelectLabelCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
    </UserControl.InputBindings>

    <UserControl.Style>
        <Style TargetType="UserControl">
            <Setter Property="Panel.ZIndex" Value="0" />
            <Style.Triggers>
                <DataTrigger Value="True">
                    <DataTrigger.Binding>
                        <MultiBinding Converter="{StaticResource ObjectEqualityConverter}">
                            <Binding />
                            <Binding Path="DataContext.SelectedLabel" RelativeSource="{RelativeSource AncestorType=Window}" />
                        </MultiBinding>
                    </DataTrigger.Binding>
                    <Setter Property="Panel.ZIndex" Value="999"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>

    <Grid>
        
        <!-- Border -->
        <Border x:Name="LabelBorder"
                BorderBrush="{Binding ClassBrush}"
                BorderThickness="1.5">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="Transparent" />
                    <Style.Triggers>
                        <DataTrigger Value="True">
                            <DataTrigger.Binding>
                                <MultiBinding Converter="{StaticResource ObjectEqualityConverter}">
                                    <Binding />
                                    <Binding Path="DataContext.SelectedLabel" RelativeSource="{RelativeSource AncestorType=Window}" />
                                </MultiBinding>
                            </DataTrigger.Binding>
                            <Setter Property="Background" Value="{Binding SemiTransparentBrush}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>

        <Grid x:Name="ThumbsContainer" Margin="-5">
            <Grid.Visibility>
                <MultiBinding Converter="{StaticResource ObjectEqualityToVisibilityConverter}">
                    <Binding />
                    <!-- This is the current Label (the DataContext of this item) -->
                    <Binding Path="DataContext.SelectedLabel" RelativeSource="{RelativeSource AncestorType=Window}" />
                </MultiBinding>
            </Grid.Visibility>

            <!-- Corner thumbs -->
            <Thumb x:Name="TopLeft" Width="10" Height="10" Style="{StaticResource ResizableThumbStyle}" HorizontalAlignment="Left" VerticalAlignment="Top" Cursor="SizeNWSE" DragDelta="TopLeftThumb_DragDelta"/>
            <Thumb x:Name="TopRight" Width="10" Height="10" Style="{StaticResource ResizableThumbStyle}" HorizontalAlignment="Right" VerticalAlignment="Top" Cursor="SizeNESW" DragDelta="TopRightThumb_DragDelta"/>
            <Thumb x:Name="BottomLeft" Width="10" Height="10" Style="{StaticResource ResizableThumbStyle}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Cursor="SizeNESW" DragDelta="BottomLeftThumb_DragDelta"/>
            <Thumb x:Name="BottomRight" Width="10" Height="10" Style="{StaticResource ResizableThumbStyle}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Cursor="SizeNWSE" DragDelta="BottomRightThumb_DragDelta"/>

            <!-- Side thumbs -->
            <Thumb x:Name="LeftThumb" Width="10" Height="10" Style="{StaticResource ResizableThumbStyle}" HorizontalAlignment="Left" VerticalAlignment="Center" Cursor="SizeWE" DragDelta="LeftThumb_DragDelta"/>
            <Thumb x:Name="RightThumb" Width="10" Height="10" Style="{StaticResource ResizableThumbStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Cursor="SizeWE" DragDelta="RightThumb_DragDelta"/>
            <Thumb x:Name="TopThumb" Width="10" Height="10" Style="{StaticResource ResizableThumbStyle}" HorizontalAlignment="Center" VerticalAlignment="Top" Cursor="SizeNS" DragDelta="TopThumb_DragDelta"/>
            <Thumb x:Name="BottomThumb" Width="10" Height="10" Style="{StaticResource ResizableThumbStyle}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Cursor="SizeNS" DragDelta="BottomThumb_DragDelta"/>
        </Grid>

    </Grid>
</UserControl>
